apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-insight.fullname" . }}-test-connection"
  labels:
    {{- include "api-insight.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-insight.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
